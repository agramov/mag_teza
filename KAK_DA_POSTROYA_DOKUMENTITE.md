# Как да построя документите локално

Нужен е само **Node.js**. Изходните текстове и скриптовете са в работната папка на сесията:

```
C:\Users\AG\AppData\Roaming\Claude\local-agent-mode-sessions\43a450e6-4ca7-499e-b44f-653beb6fd545\12acadc7-4157-4008-9117-22044b40eccd\local_9435c8a6-e8b2-453d-8b40-9922dbe1447c\outputs
```

Тя е обикновена Windows папка — просто не се показва в интерфейса на приложението.

## Стъпки (PowerShell)

```powershell
$src = "C:\Users\AG\AppData\Roaming\Claude\local-agent-mode-sessions\43a450e6-4ca7-499e-b44f-653beb6fd545\12acadc7-4157-4008-9117-22044b40eccd\local_9435c8a6-e8b2-453d-8b40-9922dbe1447c\outputs"
$out = "C:\Users\AG\OneDrive\Documents\uni\mag_teza"

cd $src
npm install docx            # само първия път
node build_mt.js  "$out\MT_Aleksandar_tekst.docx"
node build_nip.js "$out\NIP_Aleksandar.docx"
```

Ако не подадеш път като аргумент, скриптът записва до себе си в `$src`.

## Препоръчително: копирай сорсовете при себе си

Работната папка се изчиства между сесиите. За да не загубиш изходните текстове:

```powershell
robocopy $src "$out\build" *.txt *.js /NFL /NDL
```

След това строй от `$out\build` — там е и по-безопасно.

## Какви са файловете

| Файл | Какво съдържа |
|---|---|
| `build_mt.js` | сглобява магистърската теза |
| `build_nip.js` | сглобява НИП (само Глава 1 + изводи + източници) |
| `bib.txt` | библиография, формат `ключ :: пълно описание` (по един ред) |
| `g11a.txt` … `g37.txt` | текстът по подточки; абзаците се разделят с ред, съдържащ само `###` |
| `giz1.txt`, `giz2.txt` | изводите от Глава 1 и Глава 2 |
| `guvod.txt` | уводът |

## Как се цитира в изходния текст

В текста пишеш `{{ключ}}` или `{{ключ|уточнение}}`, например:

```
… както показват изследванията {{clark|гл. 13}} и {{gaft}}.
```

Скриптът сам:

1. събира кои източници реално се цитират;
2. подрежда ги **азбучно — кирилица, латиница, уеб ресурси** (Наредба на УниБИТ, чл. 54, ал. 1);
3. номерира ги и заменя `{{ключ}}` със `[номер]`;
4. отпечатва списъка накрая.

Нищо не се номерира на ръка — добавяш източник в `bib.txt` и го цитираш, останалото е автоматично.

## Приложено оформление (Наредба, чл. 47)

Times New Roman 14 pt · междуредие 1,5 · отстъп нов ред 20 мм · полета: ляво 30, дясно 10, горно и долно 15 мм · двустранно подравняване · номерация долу вдясно без първата страница.
